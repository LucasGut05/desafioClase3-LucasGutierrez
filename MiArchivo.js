let valor = Number(
  prompt("ingrese un valor para la cuenta regresiva o 0 para terminar: ")
);

while (valor!=0) {
  
  

for (let i = valor; i >= 0; i--) {
  alert(i);
}
valor = Number(
  prompt("ingrese un valor para la cuenta regresiva o 0 para terminar: ")
);

}